## Tasks

- [x] 1. `prisma/schema.prisma`: tornar `Purchase.ownerUserId` obrigatório, remover `Purchase.isShared`, adicionar model `PurchaseParticipant` (purchaseId, userId, `@@unique([purchaseId, userId])`) e as relações inversas em `Purchase`/`User`.
- [x] 2. Gerar a migration com `prisma migrate dev --create-only` e editar o SQL gerado à mão para: (a) criar a tabela `PurchaseParticipant`; (b) backfill de `Purchase.ownerUserId` usando `COALESCE(ownerUserId, Card.ownerUserId)` via join por `cardId`; (c) backfill de `PurchaseParticipant` (1 linha por compra para o dono; +1 linha por outro usuário existente para toda compra que tinha `isShared=true`); (d) tornar `ownerUserId` `NOT NULL`; (e) dropar a coluna `isShared`.
- [x] 3. Rodar a migration (`prisma migrate dev`) e `prisma generate`; conferir que `src/generated/prisma` reflete o novo schema sem `isShared` e com `ownerUserId` obrigatório.
- [x] 4. `src/lib/money.ts`: remover `userShareCents`, adicionar `splitAmongParticipants(totalCents, orderedUserIds)` construída sobre `splitValue`.
- [x] 5. `src/lib/validation/schemas.ts`: em `purchaseSchema` (herdado por `updatePurchaseSchema`), trocar `isShared: z.coerce.boolean()` por `additionalParticipantUserIds: z.array(z.string().min(1)).default([])`; adicionar `createParticipantSchema`.
- [x] 6. `src/lib/auth.ts`: adicionar `listParticipantCandidates(userId)` retornando todos os demais usuários (`id`, `name`); não alterar `getOtherUser()`.
- [x] 7. Criar `src/lib/actions/participant-actions.ts` com a action `createParticipant({ name })`: `requireUser()`, validação via `createParticipantSchema`, geração de `username` (normalizado, sem acento/espaço) com resolução de colisão por sufixo numérico, hash `bcrypt` de `<username>123`, criação do `User`, retorno de `{ id, name, username }`.
- [x] 8. `src/lib/actions/purchase-actions.ts` — `createPurchase`: validar que todo id em `additionalParticipantUserIds` existe e não contém o próprio usuário; dentro da transação, gravar `ownerUserId` e criar as linhas de `PurchaseParticipant` (`[user.userId, ...additionalParticipantUserIds]`).
- [x] 9. `src/lib/actions/purchase-actions.ts` — `updatePurchase`: mesma validação da task 8; substituir as linhas de `PurchaseParticipant` existentes (delete + createMany) mantendo a checagem de visibilidade agora baseada em `participants`.
- [x] 10. `src/lib/actions/purchase-actions.ts` — `toggleInstallmentPaid` e `deletePurchase`: trocar a checagem `isShared || ownerUserId === user.userId` por checagem contra a lista de `participants` da compra.
- [x] 11. `src/lib/actions/dashboard-actions.ts` — `getMonthlyTimeline`: trocar o filtro `OR: [{ownerUserId}, {isShared:true}]` de `purchaseInstallment` por `participants: { some: { userId } }`, incluir `participants` na query, e trocar `userShareCents(...)` por `splitAmongParticipants(...)[user.userId]`. Não alterar a query equivalente de `fixedExpenseInstallment`.
- [x] 12. Criar `src/components/participant-picker.tsx` (client): combobox de busca + seleção múltipla de participantes (reaproveitando o padrão visual de `category-combobox.tsx` e `user-badge.tsx` para os chips selecionados), com opção "Criar participante '{nome}'" quando a busca não encontra ninguém, chamando `createParticipant` e atualizando a lista local via `onCreated`.
- [x] 13. `src/app/(app)/purchases/new/page.tsx` + `purchase-form.tsx`: buscar `listParticipantCandidates`, trocar o checkbox `isShared` pelo `ParticipantPicker` ligado a `additionalParticipantUserIds`.
- [x] 14. `src/app/(app)/purchases/[id]/edit/page.tsx` + `edit-purchase-form.tsx`: mesma troca da task 13, pré-selecionando os participantes atuais da compra.
- [x] 15. `purchase-edit-modal.tsx` + `purchases-listing.tsx` (`PurchaseRow`, badge de compartilhamento, prop `otherUserName`): trocar `isShared`/`otherUserName` por uma lista de participantes (`{ id, name }[]`), atualizando o `ParticipantPicker` no modal e a exibição na listagem (tabela e cards).
- [x] 16. `src/app/(app)/purchases/page.tsx`: trocar a query de listagem para filtrar por `participants.some.userId`, incluir os participantes com nome, e buscar `listParticipantCandidates` em vez de `getOtherUser`.
- [x] 17. `src/app/(app)/purchases/[id]/page.tsx` (detalhe da compra): exibir a lista completa de participantes em vez do texto fixo de "outro usuário".
- [x] 18. `src/app/(app)/layout.tsx` + `quick-add-fab.tsx` + `quick-add-sheet.tsx`: trocar `getOtherUser` por `listParticipantCandidates`, e o checkbox `isShared` pelo `ParticipantPicker` no lançamento rápido.
- [x] 19. `src/app/(app)/simulate/page.tsx` + `simulate-form.tsx`: mesma troca da task 18 no fluxo de simulação.
- [x] 20. Validar tudo: `npm run lint`, `tsc --noEmit`, e teste manual no `npm run dev` cobrindo: criar compra com participante novo (inline) e existentes, divisão exata em centavos, edição de participantes com parcela paga, listagem/detalhe mostrando os nomes corretos, dashboard mostrando 1/N, e login funcionando com a senha `<username>123` do participante criado.
